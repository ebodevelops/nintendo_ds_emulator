#include "cpu/arm_core.h"

// Decode tables and shared interpreter helpers will land in milestone M1.
// This translation unit exists today so the build graph includes the header
// and to give later interpreter code a stable home.

namespace nds::cpu {

// (intentionally empty for now)

}  // namespace nds::cpu
